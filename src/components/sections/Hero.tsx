"use client";

import HeroCarousel from "./HeroCarousel";

const Hero = () => {
  return (
    <section className="px-5 pt-6 pb-12 md:px-8 lg:px-10 flex justify-center">
      <HeroCarousel />
    </section>
  );
};

export default Hero;
