"use client";

import HeroCarousel from "./HeroCarousel";

const Hero = () => {
  return (
    <section className="px-5 pt-10 flex justify-center">
      <HeroCarousel />
    </section>
  );
};

export default Hero;
